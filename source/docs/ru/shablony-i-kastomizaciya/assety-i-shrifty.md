# Централизованное подключение ассетов: загрузчик SF4, подключение по смыслу, порядок загрузки
# Системные vs проектные пакеты; конфиг проекта `{site_dir}/simai.data/config/.asset.config.php` замещает системный
# Где хранить CSS/JS проекта: `{site_dir}/simai.data/template` (style/js), не править `/simai` и системный шаблон
# Версионирование и кеш: обновление пакетов и очистка кеша при смене ассетов
# Шрифты: централизованный конфиг `{site_dir}/simai.data/config/.font.config.php`, подключение через загрузчик
