| Code | Назначение | Реализация | Ключевые параметры | Примечания |
| :---- | :---- | ----: | :---- | :---- |
| `iblock.export.archive` | Упаковка инфоблоков в архив | `class.php` | **Требует уточнения** (точная схема параметров) | Реализовано “тяжёлым” классом упаковки. В документации лучше фиксировать *контракт*: что на входе (список ИБ/путь), что на выходе (zip). |
| `iblock.import.archive` | Распаковка инфоблоков | `class.php` | **Требует уточнения** | Аналогично: вход (zip \+ режим), выход (созданные/обновлённые ИБ). |
| `iblock.import.archive.sveden` | Распаковка ИБ для “сведений” (структура отдельно от контента) | `action.php` | `parameter[]: source, destination, site, save?` | В конфиге мастера встречаются ключи `source`, `destination`, `site`, иногда `save`. |
| `iblock.translate` | Перевод инфоблоков | только описание | — | Устаревшее. |
| `iblockconfig.import.data` | Настройки публичного редактора инфоблока | `action.php` | В конфиге мастера встречаются `site` и `tmp` | Это шаг “прикручивает” конфиги редактора к ИБ (в терминах SF4). |
| `iblocksection.import.data` | Создание разделов инфоблока | `action.php` | **Требует уточнения** | Исполняемый код есть, но контракт параметров нужно закрепить. |
| `iblocktype.export.data` | Получение типов инфоблоков | `action.php` | (параметры формально есть, но по сути не обязательны) | Выгружает типы ИБ \+ языковые названия (языки берёт из списка сайтов). |
| `iblocktype.import.data` | Создание типов инфоблоков | только описание | — | В локальных данных нет исполняемого кода — нужно уточнить, где реально выполняется в вашей версии. |
